package com.philihp.bj;

public enum Response {
    H,  // Hit
    S,  // Stay
    DH, // Double if allowed, otherwise Hit
    DS, // Double if allowed, otherwise Stay
    P,  // Split
    RH  // Surrender if allowed, otherwise Hit
}
